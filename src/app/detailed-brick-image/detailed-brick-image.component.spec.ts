/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { DetailedBrickImageComponent } from './detailed-brick-image.component';

describe('Component: DetailedBrickImage', () => {
  it('should create an instance', () => {
    let component = new DetailedBrickImageComponent();
    expect(component).toBeTruthy();
  });
});
